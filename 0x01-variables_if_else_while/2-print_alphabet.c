#include <stdio.h>
/**
 * main - this is the forced comment i need to add
 * Return: 0 (Success)
 */

int main(void)
{
char alphabet = 'a';
while (alphabet != '{')
{
putchar(alphabet);
alphabet++;
}
putchar('\n');
return (0);
}
